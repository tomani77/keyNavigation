import { ArrowNavigationManagerDirective } from './resequence-arrow-navigation-manager.directive';

describe('ArrowNavigationManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowNavigationManagerDirective();
    expect(directive).toBeTruthy();
  });
});
